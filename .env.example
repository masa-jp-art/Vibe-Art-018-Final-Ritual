# この作品はデフォルトで外部APIを使用しません。
# もし外部API/外部モデル/外部メディアを使う場合のテンプレート例です。
# これらはフロントエンドでは直接参照せず、必ずサーバ側でプロキシしてください。

# MEDIA_BASE_URL=https://your.cdn.example
# TFJS_MODEL_URL=https://your.model.host/model.json
